using UnityEngine;

namespace LoseWeight.UI.Pages
{
    public class DressingPage : MonoBehaviour
    {
        public void ShowItems(string tab) => Debug.Log($"[DressingPage] Show items: {tab}");
    }
}
